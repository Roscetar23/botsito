import { notificationsModel } from './notifications-model.js';

describe('notificationsModel', () => {
  it('should work', () => {
    expect(notificationsModel()).toEqual('notifications-model');
  });
});
