import { notificationsFeature } from './notifications-feature.js';

describe('notificationsFeature', () => {
  it('should work', () => {
    expect(notificationsFeature()).toEqual('notifications-feature');
  });
});
