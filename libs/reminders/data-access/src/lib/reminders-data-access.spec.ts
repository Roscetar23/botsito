import { remindersDataAccess } from './reminders-data-access.js';

describe('remindersDataAccess', () => {
  it('should work', () => {
    expect(remindersDataAccess()).toEqual('reminders-data-access');
  });
});
